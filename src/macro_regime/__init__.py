"""
Macro Regime Package Initialization.
Serves as the root module for the pipeline, exposing core functions.

Key Responsibilities:
- Defines the package version.
- Allows for clean, top-level imports across the macro_regime package.

Key Functions/Variables:
- `__version__`: Current package version string.
"""

__version__ = "0.1.0"
